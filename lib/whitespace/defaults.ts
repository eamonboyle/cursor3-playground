/** Sample snippet with mixed whitespace issues for the demo textarea. */
export const SAMPLE_WHITESPACE_TEXT = `function greet(name: string) {\t
  return "hello " + name  \r
}\r
\u200B// invisible zero-width space before comment
const x = 1;\u00A0
`
